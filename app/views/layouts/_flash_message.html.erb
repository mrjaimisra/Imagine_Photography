<div class="row">
  <div class="col-lg-12">
    <% flash.each do |type, message| %>
      <div class="alert alert-<%= type %>" role="alert">
        <%= message %>
      </div>
    <% end %>
  </div>
</div>
