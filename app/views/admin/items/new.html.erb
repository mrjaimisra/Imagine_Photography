<h1>Add Photo</h1>
<%= render partial: "layouts/item_errors" %>
<div class="row">

  <div class="col-lg-12">

    <div class="admin-item-form">

      <%= form_for [:admin, @item] do |f| %>
        <div class="form-group">
          <%= f.label :name %>
          <%= f.text_field :name, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :description %>
          <%= f.text_area :description, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :price %>
          <%= f.text_field :price, class: "form-control" %>
        </div>

        <div class="form-group dropdown-toggle">
          <%= f.label :category %>
          <%= f.collection_select(:category, Category.all, :id, :name, include_blank: "Please Select") %>
        </div>

        <div class="form-group">
          <%= f.label :image_url %>
          <%= f.text_field :image_url, class: "form-control" %>
        </div>

        <div class="form-group dropdown-toggle">
          <%= f.label :status %>
          <%= f.select(:status, Item.statuses.keys.map {|status| [status.titleize,status]}) %>
        </div><br><br>
        <%= f.submit "Add Meal", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
